<?xml version="1.0" encoding="utf-8"?>
<resources>
    <item name="story_text_background_res_id" type="id" />
    <!-- Marker for non-editable story tag overlays (e.g. @username) -->
    <item name="story_tag_overlay_view" type="id" />
    <!-- Marker for non-editable story location overlays (e.g. 📍 Hyderabad, Telangana) -->
    <item name="story_location_overlay_view" type="id" />
    <!-- Marker for the first user tag overlay (for position tracking) -->
    <item name="story_user_tag_first" type="id" />
    <!-- Marker for user tag ID (stores the TagPeople.id) -->
    <item name="story_user_tag_id" type="id" />
    <!-- Marker for dynamically created user tag buttons -->
    <item name="dynamic_user_tag_button" type="id" />
    
    <!-- UCrop toolbar button IDs -->
    <item name="ucrop_fab_cancel" type="id" />
    <item name="ucrop_fab_done" type="id" />
    <item name="toolbar_title" type="id" />
</resources>

