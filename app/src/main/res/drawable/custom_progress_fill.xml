<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Filled part (white color) -->
    <item android:id="@android:id/progress">
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- White fill -->
            <corners android:radius="12dp" /> <!-- Rounded corners with radius 2dp -->
        </shape>
    </item>
</layer-list>
